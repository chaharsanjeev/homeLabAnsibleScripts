---
- hosts: all
  become: true
  tasks:
  - name: Stop service cron on debian, if running
    ansible.builtin.systemd_service:
      name: docker
      state: stopped
  - name: Stop container
    community.general.proxmox:
      vmid: 260
      api_user: root@pam
      api_password: Tuhina@0404
      api_host: vm.sc
      state: stopped
  
